{
  "name": "todo-thought-universe",
  "version": "0.2.3-rc.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc -b --noEmit",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:source-targets": "vitest run src/domain/engine/**/*.test.ts src/services/appStateTransfer.test.ts src/services/storage.test.ts src/services/applyAiPatch.test.ts src/domain/validation/appStateInvariants.test.ts src/domain/projectThoughtLinks.test.ts src/domain/projectActions.test.ts src/domain/relationships/relationshipGraph.test.ts src/domain/mutations/appMutations.test.ts src/domain/engineeringHandoff.test.ts",
    "typecheck:no-build": "tsc --noEmit --incremental false",
    "verify:source": "npm run test:source-targets && npm run typecheck:no-build",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "vitest": "latest"
  }
}
